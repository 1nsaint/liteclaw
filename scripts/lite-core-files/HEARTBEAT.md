# HEARTBEAT

Keep empty (or comments only) to skip heartbeat checks.
Add a short checklist below for periodic checks.
If nothing needs attention when the heartbeat runs, reply `HEARTBEAT_OK`.

- [ ] _(optional recurring task)_
