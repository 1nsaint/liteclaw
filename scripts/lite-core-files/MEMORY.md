# MEMORY

Long-term curated memory, main session only — don't load in group/shared contexts.
Use `memory_search` on this file and `memory/*.md` for prior context.
Write significant decisions, preferences, and lessons here.
Daily raw logs go in `memory/YYYY-MM-DD.md`.
